name: CI

on:
  pull_request:
    types: [opened, synchronize, reopened, ready_for_review]
    paths-ignore:
      - ".jules/env_setup.sh"

jobs:
  checks:
    uses: ./.github/workflows/reusable-check.yml
