---
interface Props {
  type?: 'info' | 'warning' | 'tip' | 'error';
  title?: string;
}

const { type = 'info', title } = Astro.props;

const colors = {
  info: 'bg-blue-50 dark:bg-blue-900/20 border-blue-200 dark:border-blue-800 text-blue-800 dark:text-blue-200',
  warning: 'bg-yellow-50 dark:bg-yellow-900/20 border-yellow-200 dark:border-yellow-800 text-yellow-800 dark:text-yellow-200',
  tip: 'bg-emerald-50 dark:bg-emerald-900/20 border-emerald-200 dark:border-emerald-800 text-emerald-800 dark:text-emerald-200',
  error: 'bg-red-50 dark:bg-red-900/20 border-red-200 dark:border-red-800 text-red-800 dark:text-red-200',
};

const icons = {
  info: 'ℹ️',
  warning: '⚠️',
  tip: '💡',
  error: '🚫',
};

const styles = colors[type];
const icon = icons[type];
---

<div class={`p-4 my-6 rounded-lg border flex gap-3 ${styles}`}>
  <div class="flex-shrink-0 select-none text-xl">
    {icon}
  </div>
  <div class="w-full min-w-0">
    {title && <h3 class="font-bold mb-1 text-inherit">{title}</h3>}
    <div class="prose prose-sm dark:prose-invert max-w-none text-inherit prose-p:my-0 prose-a:text-current">
      <slot />
    </div>
  </div>
</div>
