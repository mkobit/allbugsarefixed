---
import EChartReact from './EChart';

interface Props {
  options: Record<string, any>;
  height?: string;
  width?: string;
}

const { options, height = "400px", width = "100%" } = Astro.props;
---

<EChartReact client:only="react" options={options} height={height} width={width} />
